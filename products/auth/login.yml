info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseurl}}/auth/login"
  body:
    type: json
    data: '{"email": "admin@test.com", "password": "12345678"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
