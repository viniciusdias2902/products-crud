info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseurl}}/auth/register"
  body:
    type: json
    data: '{"name": "USER", "email": "user@test.com", "password": "12345678", "role": "USER"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
