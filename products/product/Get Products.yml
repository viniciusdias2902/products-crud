info:
  name: Get Products
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseurl}}/products/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
